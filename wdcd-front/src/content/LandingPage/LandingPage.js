import React from 'react';
import { Button } from 'carbon-components-react';

const LandingPage = ()  => {
    return (
        <div>
            <Button>여기가 시작페이지!</Button>
        </div>
    );
};

export default LandingPage;